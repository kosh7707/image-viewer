{
  "name": "image-viewer",
  "version": "1.1.0",
  "description": "Minimal fullscreen Electron image viewer (JPEG/PNG/WebP/GIF/EPS) for Windows",
  "main": "dist/src/main/main.js",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.js && node scripts/bundle-renderer.js",
    "start": "npm run build && electron .",
    "dev": "tsc -w & electron .",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "dist": "npm run build && electron-builder --dir && npm run portable:folder",
    "portable:folder": "node scripts/make-portable-folder.js release/win-unpacked release",
    "portable:audit": "node scripts/audit-portable-folder.js release/ImageViewerPortable",
    "startup:audit": "node scripts/audit-startup-imports.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "electron",
    "image-viewer"
  ],
  "author": "",
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.11.0",
    "electron": "42.2.0",
    "electron-builder": "26.8.1",
    "esbuild": "^0.28.0",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.60.0"
  },
  "dependencies": {
    "@bentopdf/gs-wasm": "^0.1.1",
    "gifuct-js": "^2.1.2",
    "image-size": "^2.0.2"
  }
}
